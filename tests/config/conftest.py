"""Config test fixtures — see tests/conftest.py for shared ones.

``patch_config_paths``, ``user_config_dir``, and ``project_dir`` now live in
the root ``tests/conftest.py`` so CLI command tests can reuse them.
"""
