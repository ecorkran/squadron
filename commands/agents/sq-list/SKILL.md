---
name: sq-list
description: Lists squadron's active agents with their type, state and provider. Use when the user asks what agents are running, wants to see the agent list, or asks about the state of a spawned agent.
---

# sq list

Run `sq list`, appending whatever the user typed after `$sq-list` as arguments, and display the
results.

Optional flags: `--state STATE`, `--provider PROVIDER`. Shows all active agents with their type,
state, and provider.
