name: review
description: Run a review against existing artifacts

params:
  slice: required
  model: sonnet

steps:
  - review:
      template: code
      model: "{model}"
      checkpoint: on-concerns
