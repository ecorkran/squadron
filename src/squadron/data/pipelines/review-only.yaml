name: review-only
description: Run a review against existing artifacts

params:
  slice: required
  template: required
  model: sonnet

steps:
  - review:
      template: "{template}"
      checkpoint: on-concerns
